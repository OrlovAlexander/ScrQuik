---
description: Анализ графиков одного инструмента на нескольких ТФ — RPM-up, RPM-current, 121 только на M30
alwaysApply: true
---

# Анализ графиков: один инструмент, несколько ТФ

При разборе графиков одного инструмента по разным таймфреймам соблюдать раскладку как в QUIK.

## Индикаторы

- Есть **два** индикатора RPM: `*RPM_TF_Up_5` (up) и `*RPM_TF_Current` (current).
- **One2One_121** ставится **только на M30**. На M1, M10 и H4 его нет.

## RPM-up

- Настройки общие для всех инструментов (не привязывать к sec_code).
- Брать из `RPM_TF_Up_5.ini`, секция по ТФ **графика**:
  - M1 ? `[RPM_TF_Up_5.M1]`
  - M10 ? `[RPM_TF_Up_5.M10]`
  - M30 ? `[RPM_TF_Up_5.M30]`
  - H4 ? `[RPM_TF_Up_5.H4]`
- Слои Small / Middle / Up в секции — старшие ТФ относительно графика. Не подставлять дефолты из `RPM_TF_Up_5.lua`.

## RPM-current

- Считает RPM на **текущем** ТФ графика. Не путать с up и не брать параметры up из ini.

## 121

- Считать и сопоставлять паттерн только на M30 того же инструмента.
